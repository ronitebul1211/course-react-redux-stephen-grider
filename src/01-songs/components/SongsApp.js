import React from "react";
import SongList from "./SongList";

const SongsApp = () => {
   return (
      <div className="songs-app">
         <div>
            <SongList />
         </div>
      </div>
   );
};

export default SongsApp;
